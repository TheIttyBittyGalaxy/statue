// EXPECT ERROR

// FIXME: Make error expectation more specific (currently, the test "succeeds"
//        because this test crashes the compiler, not because the correct
//        error is identified.)

funct main()
    let greeting = "Hello World"
    print(greeting)

    greeting = "Hey World"
    print(greeting)
end